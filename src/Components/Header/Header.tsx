import {HeaderComponent} from "./HeaderComponent";
import {SubHeaderComponent} from "./SubHeaderComponent";

export const Header = () => {
    return (
        <header>
            <HeaderComponent />
            <SubHeaderComponent />
        </header>
    )
}